const uploader = document.getElementById('upload-button');
const harvestBtn = document.querySelector('.harvest .-button');
const textarea = document.querySelector('textarea');
const txtContainer = document.querySelector('.textarea');
const dropBox = document.querySelector('.dropzone__box');
const dropZone = document.querySelector('.dropzone');
const resultsContainer = document.querySelector('.results');
const resultsNa = document.querySelector('.results__na');
const tryagain = document.querySelector('.tryagain');
const tryagainBtn = document.querySelector('.tryagain .-button');

export { uploader, tryagainBtn, textarea, dropBox, dropZone, resultsContainer, resultsNa, tryagain, txtContainer, harvestBtn };
